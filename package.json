{
  "name": "resistar",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "docker": "docker-compose up",
    "docker-stop": "docker-compose down",
    "deploy-front": "git subtree push --prefix frontend/ front main"
  }
}
